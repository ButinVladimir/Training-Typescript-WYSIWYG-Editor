import { InputStyleView } from "../base";

const title: string = "Padding top";

export class PaddingTopStyleView extends InputStyleView {
    public getTitle(): string {
        return title;
    }
}