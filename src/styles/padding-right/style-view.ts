import { InputStyleView } from "../base";

const title: string = "Padding right";

export class PaddingRightStyleView extends InputStyleView {
    public getTitle(): string {
        return title;
    }
}