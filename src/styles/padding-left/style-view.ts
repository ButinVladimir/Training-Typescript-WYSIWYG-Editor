import { InputStyleView } from "../base";

const title: string = "Padding left";

export class PaddingLeftStyleView extends InputStyleView {
    public getTitle(): string {
        return title;
    }
}