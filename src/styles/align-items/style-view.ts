import { SelectStyleView } from "../base";

const title: string = "Align items";

export class AlignItemsStyleView extends SelectStyleView {
    public getTitle(): string {
        return title;
    }
}