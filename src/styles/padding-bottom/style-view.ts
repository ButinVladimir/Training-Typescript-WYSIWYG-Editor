import { InputStyleView } from "../base";

const title: string = "Padding bottom";

export class PaddingBottomStyleView extends InputStyleView {
    public getTitle(): string {
        return title;
    }
}